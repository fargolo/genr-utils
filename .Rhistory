teste[1,] <- paste("Idade")
row.names(teste) <- "Idade"
row.names(teste) <- c("Idade)"
)
row.names(teste) <- c("Idade")
teste <- data.frame()
teste
row.names(teste) <- c("Idade")
teste <- data.frame(x=1,y=1:10)
teste
teste <- data.frame(Total = Idade.Total, Pacientes = Idade.Pacientes, Controles = Idade.Controles)
teste
row.names(teste) <- "Idade"
teste
Tabela1 <- vanilla.table(teste)
resThi <- AddFlexTable(doc,Tabela1)
resThi <- addFlexTable(doc,Tabela1)
resThi <- addFlexTable(resThi,Tabela1)
writeDoc (resThi, file = "Resultados - Thiago.docx")
getwd()
can072 <- read.csv("C:/Users/Argolo/RWork/can072.csv", sep=";", dec=",")
View(can072)
ACC071 <- read.csv("C:/Users/Argolo/Dropbox/H1 Estatistica/H1 Consultoria/Projetos/T1 - Em andamento/Mariana Guimaraens - Analise de Correspondencia/ACC071.csv", sep=";", dec=",")
View(ACC071)
ACC071 <- ACC071[1,]
View(ACC071)
ACC071 <- read.csv("C:/Users/Argolo/Dropbox/H1 Estatistica/H1 Consultoria/Projetos/T1 - Em andamento/Mariana Guimaraens - Analise de Correspondencia/ACC071.csv", sep=";", dec=",")
View(ACC071)
ACC071 <- ACC071[,-1]
View(ACC071)
row.names(ACC071) <- c("Jan","Feb","Mar","Apr", "May","Jun")
View(ACC071)
ACC071
library(JGR)
JGR()
frequencies
frequencies()
frequencies()
a = 1
b = 2
a
cor(a,b)
corr(a,b)
cor
x <- 4L
class(x)
x +1
x <- c(4, "a", TRUE)
class(x)
x[[3]]
x <- 4L
x
swirl
swirl()
library(swirl)
install.packages("swirl")
swirl()
library(swirl)
swirl()
5+7
x <- 5+7
x
y <- x-3
y
z <- c(1.1,9,3.14)
?c
z
c(z,555,z)
z*2+100
my_sqrt <- sqrt(z-1)
my_sqrt
my_div <- z/my_sqrt
my_div
c(1, 2, 3, 4)+c(0, 10)
c(1, 2, 3, 4) + c(0, 10, 100)
c(1, 2, 3, 4) + c(0, 10, 100)
z*2+100
z*2+1000
getwd()
getwd()
dir()
setwd("C:\\Users\\Argolo\\Dropbox\\H1 Estatistica\\H1 Consultoria\\Codigos e softwaes auxiliares\\H1stats")
getwd()
dir()
tableMeans.R
load(TableMeans.R)
read(TableMeans.R)
open(TabelMeans.R)
source(TableMeans.R)
source('C:/Users/Argolo/Dropbox/H1 Estatistica/H1 Consultoria/Codigos e softwaes auxiliares/H1stats/tableMeans2.R')
tableMeans()
help(tableMeans)
data.frame()
teste <- data.frame ()
teste
help(data.frame)
teste <- data.frame (nrow=1)
teste
vars <- c("var1","var2","var3")
class(vars)
len(vars)
lenght(vars)
lengh(vars)
length(vars)
Mydata
View(Banco)
mean(Banco$idade)
freq
freq()
frequencies()
summary()
summary(Banco$idade)
summary(Banco$sexo)
dim(summary(Banco$sexo))
lenght(summary(Banco$sexo))
length(summary(Banco$sexo))
class(Banco$sexo)
class(Banco$idade)
class(Banco$medicacao)
class(Banco$medicaca)
summary(Banco$medcaca)
summary(Banco$medicaca)
c("medicaca","sexo")
var.cat <- c("medicaca","sexo")
var.cat
summary(Banco[var.cat])
lenght(summary(Banco[var.cat]))
length(summary(Banco[var.cat]))
length(summary(Banco[var.cat])) + length(var.cat)
length(summary(Banco[var.cat])) + length(var.cat) + 1
length(Grupo)
length(grupo)
length(Banco$grupo)
length(summary(Banco$grupo))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- data.frame (nrow = row.len, ncol = col.len)
}
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- data.frame (nrow = row.len, ncol = col.len)
return(final.tab)
}
h1wordtab <- function (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
)
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- data.frame (nrow = row.len, ncol = col.len)
return(final.tab)
}
h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
final.tab
final.tab <- data.frame (nrow = 8, ncol = 5)
final.tab
mean(final.tab$nrow)
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
return(final.tab)
}
h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
final.tab <- matrix (nrow = 8, ncol = 5)
final.tab[1,1]
final.tab[1,1] <- "var"
final.tab[1,1]
class(final.tab[1,1])
final.tab[1,1] <- "Variáveis"
final.tab[1,1]
row.length
row.len
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- "Variables"
final.tab[1,2] <- paste("Total(n=",nrow(data),")")
return(final.tab)
}
h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- list("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
return(final.tab)
}
h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
return(final.tab)
}
h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
return(final.tab)
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
}
h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
return(final.tab)
}
h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
nrow(Banco)
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
tabtest
class(tabtest[1,2])
tabtest[1,2]
factors(Banco$grupo)
summary(Banco$grupo)
for (i in 1:length(Banco$grupo)) {}
length(summary(Banco$grupo))
Banco$grupo
Banco$grupo[1]
Banco$grupo[2]
Banco$grupo[3]
Banco$grupo[5]
Banco$grupo[6]
Banco$grupo[20]
as.factor(Banco$grupo)
class(Banco$grupo)
levels(Banco$grupo)
levels(Banco$grupo)[1]
levels(Banco[grupo])[1]
levels(Banco[grupo][1])
levels(Banco[[grupo][1])
levels(Banco[[grupo][1])
levels(Banco[[grupo]][1])
levels(Banco["grupo"][1])
levels(Banco["grupo"])
(Banco["grupo"]
)
Banco["grupo"]
levels(Banco["grupo"])
levels(Banco$grupo)
levels(as.factor(Banco["grupo"]))
levels(Banco["grupo"])
levels(Banco[["grupo"])
levels(Banco[["grupo"]])
levels(Banco[["grupo"]][1])
levels(Banco[["grupo"]])[1]
summary(Banco$group)
summary(Banco$grupo)
summary(Banco$grupo)[1]
summary(Banco$grupo)[1] + 1
summary(Banco$grupo)[[1]] + 1
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
return(final.tab)
cur.col <- 3
final.tab[1,cur.col] <- paste(levels(data[[group.var]])[1],"(n = ",summary(data[grupo][[1]]),")")
}
Ban
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste(levels(data[[group.var]])[1],"(n = ",summary(data[grupo][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste(levels(data[[var.group]])[1],"(n = ",summary(data[grupo][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste(levels(data[[var.group]])[1],"(n = ",summary(data[var.group][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste(levels(data[[var.group]])[1],"(n = ",summary(data[var.group][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,3] <- paste(levels(data[[var.group]])[1],"(n = ",summary(data[var.group][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
levels(Banco[["grupo"]])[1]
summary(Banco["grupo"][[1]])
summary(Banco[["grupo"]][[1]])
summary(Banco[["grupo"]][1])
summary(Banco["grupo"][1])
summary(Banco["grupo"])
summary(Banco["grupo"])[1]
summary(Banco["grupo"])[[1]]
summary(Banco["grupo"])[[[1]]]
summary(Banco["grupo"])[[[1]]]]
summary(Banco["grupo"])[[[1]]]
summary(Banco["grupo"])[[1]]
summary(Banco["grupo"])
summary(Banco["grupo"])[1]
summary(Banco["grupo"])[1][2]
summary(Banco["grupo"])[1][1
]
summary(Banco["grupo"])[1]
library(plyr)
count(Banco,vars="grupo")
count(Banco,vars="grupo")[1,2]
table(Banco$grupo)
table(Banco$grupo)[1]
table(Banco$grupo)[[1]]
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste(levels(data[[var.group]])[1],"(n = ",table(data[[var.group]][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
levels(Banco["grupo"])[1]
levels(Banco[["grupo"]])[1]
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste(levels(data[var.group])[1],"(n = ",table(data[[var.group]][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
levels(Banco["grupo"])[1]
levels(Banco[["grupo"]])[1]
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste(levels(data[[var.group]])[1],"(n = ",table(data[[var.group]][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste("(n = ",table(data[[var.group]][[1]]),")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
h1wordtab <- function (data,var.num,var.cat,var.group, dec.plat= 1,dec.p = 3){
#Defines cathegorical variables row length: 1 line for each cathegory + 1 line for variable name
len.cat <- length(summary(data[var.cat])) + length(var.cat)
#Defines row length: 1 for each numerical var + catheg var row len + 1 (Heading)
row.len <- length(var.num) + len.cat + 1
#Defines col lenght: one for each group + 1 for total + 1 for Variable names + 1 for p values
col.len <- length(summary(data[var.group])) + 3
#Defines lenght of final table
final.tab <- matrix (nrow = row.len, ncol = col.len)
final.tab[1,1] <- c("Variables")
final.tab[1,2] <- paste("Total(n =",nrow(data),")")
cur.col <- 3
final.tab[1,cur.col] <- paste(")")
return(final.tab)
}
tabtest <- h1wordtab (Banco, var.num = c("idade"), var.cat = c("sexo","medicaca"),var.group = c("grupo"))
tabtest
